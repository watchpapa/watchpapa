{
  "name": "watchpapa",
  "version": "2.0.0",
  "description": "watchpapa — root holds DB migrations + RLS tests. App = Frontend/ (Cloudflare Pages) + worker/ (Cloudflare Worker API).",
  "type": "module",
  "private": true,
  "scripts": {
    "test:rls": "node --test \"tests/rls_tests/**/*.test.js\"",
    "test:py": "pytest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "dotenv": "^16.4.5",
    "pg": "^8.13.0"
  }
}
